(match *.txt):
  txt_match:
    parent_value: "from child"
    new_value: 10

(match *.png):
  png_match:
    second_level:
      parent_value: "from child"
      new_value: 20

common: "overwritten"

